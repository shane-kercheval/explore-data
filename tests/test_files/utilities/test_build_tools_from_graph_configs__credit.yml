- description: (box) Shows the distribution of x axis variable (which can be a column
    of type numeric). Boxplots are useful for understanding the distribution of the
    data and identifying outliers.
  name: 19751f3e-96af-4aab-9c16-5b54e4f7ee62
  parameters:
    properties:
      color_variable:
        description: color axis that supports date, boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      facet_variable:
        description: facet axis that supports date, boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      x_variable:
        description: x axis that supports numeric columns
        enum:
        - months_loan_duration
        - amount
        - percent_of_income
        - years_at_residence
        - age
        - existing_loans_count
        - dependents
        type: string
    required: &id001
    - x_variable
    type: object
- description: (histogram) Shows the distribution of x axis variable (which can be
    a column of type numeric). Histograms are useful for understanding the distribution
    of at a more granular level compared to boxplots.
  name: 078e9a83-492a-4515-b4d4-780c0943c40a
  parameters:
    properties:
      color_variable:
        description: color axis that supports date, boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      facet_variable:
        description: facet axis that supports date, boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      x_variable:
        description: x axis that supports numeric columns
        enum:
        - months_loan_duration
        - amount
        - percent_of_income
        - years_at_residence
        - age
        - existing_loans_count
        - dependents
        type: string
    required: *id001
    type: object
- description: (scatter) Shows the relationship between x axis variable (which can
    be a column of type numeric) and y axis variable (which can be a column of type
    numeric).
  name: 91413b1e-937f-479c-aa88-ac042844329d
  parameters:
    properties:
      color_variable:
        description: color axis that supports numeric, date, boolean, string, categorical
          columns
        enum:
        - checking_balance
        - months_loan_duration
        - credit_history
        - purpose
        - amount
        - savings_balance
        - employment_duration
        - percent_of_income
        - years_at_residence
        - age
        - other_credit
        - housing
        - existing_loans_count
        - job
        - dependents
        - phone
        - default
        type: string
      facet_variable:
        description: facet axis that supports date, boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      size_variable:
        description: size axis that supports numeric columns
        enum:
        - months_loan_duration
        - amount
        - percent_of_income
        - years_at_residence
        - age
        - existing_loans_count
        - dependents
        type: string
      x_variable:
        description: x axis that supports numeric columns
        enum:
        - months_loan_duration
        - amount
        - percent_of_income
        - years_at_residence
        - age
        - existing_loans_count
        - dependents
        type: string
      y_variable:
        description: y axis that supports numeric columns
        enum:
        - months_loan_duration
        - amount
        - percent_of_income
        - years_at_residence
        - age
        - existing_loans_count
        - dependents
        type: string
    required:
    - x_variable
    - y_variable
    type: object
- description: (scatter-3d) Shows the relationship between x axis variable (which
    can be a column of type numeric), y axis variable (which can be a column of type
    numeric), and z axis variable (which can be a column of type numeric). A 3D scatter
    plot is the best option when there are three numeric variables.
  name: 98c68eed-658d-4b2c-b117-cfbe9edbf199
  parameters:
    properties:
      color_variable:
        description: color axis that supports numeric, date, boolean, string, categorical
          columns
        enum:
        - checking_balance
        - months_loan_duration
        - credit_history
        - purpose
        - amount
        - savings_balance
        - employment_duration
        - percent_of_income
        - years_at_residence
        - age
        - other_credit
        - housing
        - existing_loans_count
        - job
        - dependents
        - phone
        - default
        type: string
      size_variable:
        description: size axis that supports numeric columns
        enum:
        - months_loan_duration
        - amount
        - percent_of_income
        - years_at_residence
        - age
        - existing_loans_count
        - dependents
        type: string
      x_variable:
        description: x axis that supports numeric columns
        enum:
        - months_loan_duration
        - amount
        - percent_of_income
        - years_at_residence
        - age
        - existing_loans_count
        - dependents
        type: string
      y_variable:
        description: y axis that supports numeric columns
        enum:
        - months_loan_duration
        - amount
        - percent_of_income
        - years_at_residence
        - age
        - existing_loans_count
        - dependents
        type: string
      z_variable:
        description: z axis that supports numeric columns
        enum:
        - months_loan_duration
        - amount
        - percent_of_income
        - years_at_residence
        - age
        - existing_loans_count
        - dependents
        type: string
    required:
    - x_variable
    - y_variable
    - z_variable
    type: object
- description: (histogram) Shows the counts of x axis variable (which can be a column
    of type boolean, string, categorical). Histograms are useful for understanding
    the distribution of counts of non-numeric data.
  name: 2a89cd22-2da2-4360-9380-a36be1929755
  parameters:
    properties:
      color_variable:
        description: color axis that supports numeric, date, boolean, string, categorical
          columns
        enum:
        - checking_balance
        - months_loan_duration
        - credit_history
        - purpose
        - amount
        - savings_balance
        - employment_duration
        - percent_of_income
        - years_at_residence
        - age
        - other_credit
        - housing
        - existing_loans_count
        - job
        - dependents
        - phone
        - default
        type: string
      facet_variable:
        description: facet axis that supports date, boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      x_variable:
        description: x axis that supports boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
    required:
    - x_variable
    type: object
- description: (histogram) Shows the aggregation of y axis variable (which can be
    a column of type numeric) across each value of x axis variable (which can be a
    column of type boolean, string, categorical).
  name: 99535121-8979-4dc6-8aae-0c748f86b2b2
  parameters:
    properties:
      color_variable:
        description: color axis that supports numeric, date, boolean, string, categorical
          columns
        enum:
        - checking_balance
        - months_loan_duration
        - credit_history
        - purpose
        - amount
        - savings_balance
        - employment_duration
        - percent_of_income
        - years_at_residence
        - age
        - other_credit
        - housing
        - existing_loans_count
        - job
        - dependents
        - phone
        - default
        type: string
      facet_variable:
        description: facet axis that supports date, boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      numeric_aggregation:
        description: This is the aggregation that will be used to aggregate the numeric
          variable. Use `sum` as the default value.
        enum:
        - sum
        - avg
        - min
        - max
        type: string
      x_variable:
        description: x axis that supports boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      y_variable:
        description: y axis that supports numeric columns
        enum:
        - months_loan_duration
        - amount
        - percent_of_income
        - years_at_residence
        - age
        - existing_loans_count
        - dependents
        type: string
    required:
    - x_variable
    - y_variable
    type: object
- description: (P(Y | X)) Shows the probability of y axis variable (which can be a
    column of type boolean, string, categorical) given each value of x axis variable
    (which can be a column of type boolean, string, categorical).
  name: e66b88b3-4c8d-4cd3-9f23-3c7274e02c1e
  parameters:
    properties:
      facet_variable:
        description: facet axis that supports boolean, string, categorical, numeric
          columns
        enum:
        - checking_balance
        - months_loan_duration
        - credit_history
        - purpose
        - amount
        - savings_balance
        - employment_duration
        - percent_of_income
        - years_at_residence
        - age
        - other_credit
        - housing
        - existing_loans_count
        - job
        - dependents
        - phone
        - default
        type: string
      x_variable:
        description: x axis that supports boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      y_variable:
        description: y axis that supports boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
    required: &id002
    - x_variable
    - y_variable
    type: object
- description: (bar - count distinct) Shows the count of unique values of y axis variable
    (which can be a column of type boolean, string, categorical) for each x axis variable
    (which can be a column of type boolean, string, categorical).
  name: 5bb74459-45b5-4fb2-bd06-71810d577bfd
  parameters:
    properties:
      color_variable:
        description: color axis that supports boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      facet_variable:
        description: facet axis that supports boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      x_variable:
        description: x axis that supports boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      y_variable:
        description: y axis that supports boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
    required: *id002
    type: object
- description: (heatmap) Heatmap of the aggregation of x axis variable (which can
    be a column of type boolean, string, categorical), y axis variable (which can
    be a column of type boolean, string, categorical), and z axis variable (which
    can be a column of type numeric). This plot shows the aggregation of a numeric
    variable across two non-numeric variables.
  name: 93e6cd1b-2739-4804-b601-c87183f45f55
  parameters:
    properties:
      facet_variable:
        description: facet axis that supports boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      numeric_aggregation:
        description: This is the aggregation that will be used to aggregate the numeric
          variable. Use `sum` as the default value.
        enum:
        - sum
        - avg
        - min
        - max
        type: string
      x_variable:
        description: x axis that supports boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      y_variable:
        description: y axis that supports boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      z_variable:
        description: z axis that supports numeric columns
        enum:
        - months_loan_duration
        - amount
        - percent_of_income
        - years_at_residence
        - age
        - existing_loans_count
        - dependents
        type: string
    required:
    - x_variable
    - y_variable
    - z_variable
    type: object
- description: (heatmap - count distinct) Heatmap showing the count of distinct values
    of z axis variable (which can be a column of type boolean, string, categorical)
    for each combination of x axis variable (which can be a column of type boolean,
    string, categorical), y axis variable (which can be a column of type boolean,
    string, categorical). This plot shows the count of distinct values of a non-numeric
    variable across two non-numeric variables.
  name: 81968d6a-32af-45a0-8294-dd91ab267a98
  parameters:
    properties:
      facet_variable:
        description: facet axis that supports boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      x_variable:
        description: x axis that supports boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      y_variable:
        description: y axis that supports boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      z_variable:
        description: z axis that supports boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
    required:
    - x_variable
    - y_variable
    - z_variable
    type: object

- description: (box) Shows the distribution of x axis variable (which can be a column
    of type numeric). Boxplots are useful for understanding the distribution of the
    data and identifying outliers.
  name: e4f5e595-1893-4587-85d2-68876d58049f
  parameters:
    properties:
      color_variable:
        description: color axis that supports date, boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      facet_variable:
        description: facet axis that supports date, boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      x_variable:
        description: x axis that supports numeric columns
        enum:
        - months_loan_duration
        - amount
        - percent_of_income
        - years_at_residence
        - age
        - existing_loans_count
        - dependents
        type: string
    required: &id001
    - x_variable
    type: object
- description: (histogram) Shows the distribution of x axis variable (which can be
    a column of type numeric). Histograms are useful for understanding the distribution
    of at a more granular level compared to boxplots.
  name: 90d1f737-0ce5-4cf9-88c0-3a56702bcd06
  parameters:
    properties:
      color_variable:
        description: color axis that supports date, boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      facet_variable:
        description: facet axis that supports date, boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      x_variable:
        description: x axis that supports numeric columns
        enum:
        - months_loan_duration
        - amount
        - percent_of_income
        - years_at_residence
        - age
        - existing_loans_count
        - dependents
        type: string
    required: *id001
    type: object
- description: (scatter) Shows the relationship between x axis variable (which can
    be a column of type numeric) and y axis variable (which can be a column of type
    numeric).
  name: 28dab3e2-e31b-4b14-ba40-95385eebaa5a
  parameters:
    properties:
      color_variable:
        description: color axis that supports numeric, date, boolean, string, categorical
          columns
        enum:
        - checking_balance
        - months_loan_duration
        - credit_history
        - purpose
        - amount
        - savings_balance
        - employment_duration
        - percent_of_income
        - years_at_residence
        - age
        - other_credit
        - housing
        - existing_loans_count
        - job
        - dependents
        - phone
        - default
        type: string
      facet_variable:
        description: facet axis that supports date, boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      size_variable:
        description: size axis that supports numeric columns
        enum:
        - months_loan_duration
        - amount
        - percent_of_income
        - years_at_residence
        - age
        - existing_loans_count
        - dependents
        type: string
      x_variable:
        description: x axis that supports numeric columns
        enum:
        - months_loan_duration
        - amount
        - percent_of_income
        - years_at_residence
        - age
        - existing_loans_count
        - dependents
        type: string
      y_variable:
        description: y axis that supports numeric columns
        enum:
        - months_loan_duration
        - amount
        - percent_of_income
        - years_at_residence
        - age
        - existing_loans_count
        - dependents
        type: string
    required:
    - x_variable
    - y_variable
    type: object
- description: (scatter-3d) Shows the relationship between x axis variable (which
    can be a column of type numeric), y axis variable (which can be a column of type
    numeric), and z axis variable (which can be a column of type numeric). A 3D scatter
    plot is the best option when there are three numeric variables.
  name: acee11a5-8e8e-4016-a2a9-f703d0dfd7a6
  parameters:
    properties:
      color_variable:
        description: color axis that supports numeric, date, boolean, string, categorical
          columns
        enum:
        - checking_balance
        - months_loan_duration
        - credit_history
        - purpose
        - amount
        - savings_balance
        - employment_duration
        - percent_of_income
        - years_at_residence
        - age
        - other_credit
        - housing
        - existing_loans_count
        - job
        - dependents
        - phone
        - default
        type: string
      size_variable:
        description: size axis that supports numeric columns
        enum:
        - months_loan_duration
        - amount
        - percent_of_income
        - years_at_residence
        - age
        - existing_loans_count
        - dependents
        type: string
      x_variable:
        description: x axis that supports numeric columns
        enum:
        - months_loan_duration
        - amount
        - percent_of_income
        - years_at_residence
        - age
        - existing_loans_count
        - dependents
        type: string
      y_variable:
        description: y axis that supports numeric columns
        enum:
        - months_loan_duration
        - amount
        - percent_of_income
        - years_at_residence
        - age
        - existing_loans_count
        - dependents
        type: string
      z_variable:
        description: z axis that supports numeric columns
        enum:
        - months_loan_duration
        - amount
        - percent_of_income
        - years_at_residence
        - age
        - existing_loans_count
        - dependents
        type: string
    required:
    - x_variable
    - y_variable
    - z_variable
    type: object
- description: (histogram) Shows the counts of x axis variable (which can be a column
    of type boolean, string, categorical). Histograms are useful for understanding
    the distribution of counts of non-numeric data.
  name: c6bffc15-2b95-4a9c-a031-308b0e5966a6
  parameters:
    properties:
      color_variable:
        description: color axis that supports numeric, date, boolean, string, categorical
          columns
        enum:
        - checking_balance
        - months_loan_duration
        - credit_history
        - purpose
        - amount
        - savings_balance
        - employment_duration
        - percent_of_income
        - years_at_residence
        - age
        - other_credit
        - housing
        - existing_loans_count
        - job
        - dependents
        - phone
        - default
        type: string
      facet_variable:
        description: facet axis that supports date, boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      x_variable:
        description: x axis that supports boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
    required:
    - x_variable
    type: object
- description: (histogram) Shows the sum of y axis variable (which can be a column
    of type numeric) across each value of x axis variable (which can be a column of
    type boolean, string, categorical).
  name: d4419ce1-009b-4c42-934c-28557b408fba
  parameters:
    properties:
      color_variable:
        description: color axis that supports numeric, date, boolean, string, categorical
          columns
        enum:
        - checking_balance
        - months_loan_duration
        - credit_history
        - purpose
        - amount
        - savings_balance
        - employment_duration
        - percent_of_income
        - years_at_residence
        - age
        - other_credit
        - housing
        - existing_loans_count
        - job
        - dependents
        - phone
        - default
        type: string
      facet_variable:
        description: facet axis that supports date, boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      numeric_aggregation:
        description: This is the aggregation that will be used to aggregate the numeric
          variable. Use `sum` as the default value.
        enum:
        - sum
        - avg
        - min
        - max
        type: string
      x_variable:
        description: x axis that supports boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      y_variable:
        description: y axis that supports numeric columns
        enum:
        - months_loan_duration
        - amount
        - percent_of_income
        - years_at_residence
        - age
        - existing_loans_count
        - dependents
        type: string
    required:
    - x_variable
    - y_variable
    type: object
- description: (P(Y | X)) Shows the probability of y axis variable (which can be a
    column of type boolean, string, categorical) given each value of x axis variable
    (which can be a column of type boolean, string, categorical).
  name: 686af982-9428-43b5-8306-093aa8ec6e08
  parameters:
    properties:
      facet_variable:
        description: facet axis that supports boolean, string, categorical, numeric
          columns
        enum:
        - checking_balance
        - months_loan_duration
        - credit_history
        - purpose
        - amount
        - savings_balance
        - employment_duration
        - percent_of_income
        - years_at_residence
        - age
        - other_credit
        - housing
        - existing_loans_count
        - job
        - dependents
        - phone
        - default
        type: string
      x_variable:
        description: x axis that supports boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      y_variable:
        description: y axis that supports boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
    required: &id002
    - x_variable
    - y_variable
    type: object
- description: (bar - count distinct) Shows the count of unique values of y axis variable
    (which can be a column of type boolean, string, categorical) for each x axis variable
    (which can be a column of type boolean, string, categorical).
  name: c700a17e-d4f4-4ae1-a599-9871235af033
  parameters:
    properties:
      color_variable:
        description: color axis that supports boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      facet_variable:
        description: facet axis that supports boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      x_variable:
        description: x axis that supports boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      y_variable:
        description: y axis that supports boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
    required: *id002
    type: object
- description: (heatmap) Heatmap of the aggregation of x axis variable (which can
    be a column of type boolean, string, categorical), y axis variable (which can
    be a column of type boolean, string, categorical), and z axis variable (which
    can be a column of type numeric). This plot shows the aggregation of a numeric
    variable across two non-numeric variables.
  name: e019a558-6075-4890-9180-9983a1f0f6d6
  parameters:
    properties:
      facet_variable:
        description: facet axis that supports boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      numeric_aggregation:
        description: This is the aggregation that will be used to aggregate the numeric
          variable. Use `sum` as the default value.
        enum:
        - sum
        - avg
        - min
        - max
        type: string
      x_variable:
        description: x axis that supports boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      y_variable:
        description: y axis that supports boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      z_variable:
        description: z axis that supports numeric columns
        enum:
        - months_loan_duration
        - amount
        - percent_of_income
        - years_at_residence
        - age
        - existing_loans_count
        - dependents
        type: string
    required:
    - x_variable
    - y_variable
    - z_variable
    type: object
- description: (heatmap - count distinct) Heatmap showing the count of distinct values
    of z axis variable (which can be a column of type boolean, string, categorical)
    for each combination of x axis variable (which can be a column of type boolean,
    string, categorical), y axis variable (which can be a column of type boolean,
    string, categorical). This plot shows the count of distinct values of a non-numeric
    variable across two non-numeric variables.
  name: 8e8a979c-a9cd-4c07-8c6b-78b9d047544d
  parameters:
    properties:
      facet_variable:
        description: facet axis that supports boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      x_variable:
        description: x axis that supports boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      y_variable:
        description: y axis that supports boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
      z_variable:
        description: z axis that supports boolean, string, categorical columns
        enum:
        - checking_balance
        - credit_history
        - purpose
        - savings_balance
        - employment_duration
        - other_credit
        - housing
        - job
        - phone
        - default
        type: string
    required:
    - x_variable
    - y_variable
    - z_variable
    type: object
